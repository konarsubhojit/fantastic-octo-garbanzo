# Client Environment Variables

# Database (PostgreSQL)
DATABASE_URL=postgresql://user:password@localhost:5432/ecommerce?schema=public

# Redis Connection (use Upstash Redis for serverless)
REDIS_URL=your-redis-url

# NextAuth Configuration
NEXTAUTH_SECRET=your-nextauth-secret-generate-with-openssl-rand-base64-32
NEXTAUTH_URL=http://localhost:3000

# Google OAuth (Get from Google Cloud Console)
GOOGLE_CLIENT_ID=your-google-client-id
GOOGLE_CLIENT_SECRET=your-google-client-secret

# Vercel Blob (for image storage)
BLOB_READ_WRITE_TOKEN=your-vercel-blob-read-write-token

# QStash (Upstash serverless message queue)
QSTASH_TOKEN=your-qstash-token
QSTASH_CURRENT_SIGNING_KEY=your-current-signing-key
QSTASH_NEXT_SIGNING_KEY=your-next-signing-key

# Service URLs (deployed microservices)
# For local development, use localhost URLs
# For production, use deployed service URLs
QSTASH_ORDERS_URL=http://localhost:3001/api/webhook
QSTASH_EMAIL_URL=http://localhost:3002/api/webhook
QSTASH_INVENTORY_URL=http://localhost:3003/api/webhook
QSTASH_ANALYTICS_URL=http://localhost:3004/api/webhook

# Email Service (SMTP)
SMTP_HOST=
SMTP_PORT=587
SMTP_SECURE=false
SMTP_USER=
SMTP_PASS=
EMAIL_FROM=noreply@your-domain.com

# CORS Configuration (if API routes need CORS)
CORS_ALLOWED_ORIGINS=*
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS
CORS_ALLOWED_HEADERS=Content-Type,Authorization,X-Requested-With,Accept,Origin
CORS_MAX_AGE=86400
