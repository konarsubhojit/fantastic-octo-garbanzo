# Orders Service Environment Variables

# Database (shared with main app)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/ecommerce?schema=public

# QStash Configuration
QSTASH_TOKEN=your-qstash-token
QSTASH_CURRENT_SIGNING_KEY=your-current-signing-key
QSTASH_NEXT_SIGNING_KEY=your-next-signing-key

# Service URLs (where to publish events)
QSTASH_ORDERS_URL=https://orders-service.vercel.app/api/webhook
QSTASH_EMAIL_URL=https://email-service.vercel.app/api/webhook
QSTASH_INVENTORY_URL=https://inventory-service.vercel.app/api/webhook
QSTASH_ANALYTICS_URL=https://analytics-service.vercel.app/api/webhook

# CORS Configuration
# Comma-separated list of allowed origins (use * for all, or specify domains)
CORS_ALLOWED_ORIGINS=*
# CORS_ALLOWED_ORIGINS=https://yourdomain.com,https://app.yourdomain.com

# Comma-separated list of HTTP methods
CORS_ALLOWED_METHODS=GET,POST,PUT,DELETE,OPTIONS

# Comma-separated list of allowed headers
CORS_ALLOWED_HEADERS=Content-Type,Authorization,X-Requested-With,Accept,Origin,Upstash-Signature,X-Event-Type,X-Event-Id,X-Correlation-Id

# Preflight cache duration in seconds
CORS_MAX_AGE=86400
